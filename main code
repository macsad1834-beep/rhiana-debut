<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0"/>
<title>Rhiana's 18th Debut – You're Invited</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400;1,600&family=Cinzel+Decorative:wght@400;700&family=Cinzel:wght@400;600&family=Montserrat:wght@300;400;500&display=swap" rel="stylesheet"/>

<style>
*{margin:0;padding:0;box-sizing:border-box;}
:root{
  --ink:#0a0f2e;
  --royal:#1a2a6c;
  --sapphire:#1e3a8a;
  --gold:#c9a84c;
  --gold-light:#e8d48a;
  --gold-pale:#f5e9c4;
  --silver:#c0c8d8;
  --cream:#fdf8f0;
  --parchment:#f7f0e3;
}
html,body{width:100%;height:100%;overflow:hidden;background:var(--ink);}

/* ── PARTICLE CANVAS ── */
#particles{position:fixed;inset:0;z-index:0;pointer-events:none;}

/* ── SCENE 1: ENVELOPE ── */
#scene-envelope{
  position:fixed;inset:0;z-index:10;
  display:flex;flex-direction:column;align-items:center;justify-content:center;
  gap:2rem;
  background:radial-gradient(ellipse 90% 70% at 50% 40%,rgba(26,42,108,0.6),transparent 70%),
             linear-gradient(160deg,#060b22 0%,#0a0f2e 50%,#060f30 100%);
}
.prompt-text{
  font-family:'Cinzel',serif;
  font-size:clamp(0.7rem,2vw,0.9rem);
  letter-spacing:0.35em;
  color:var(--gold);
  text-transform:uppercase;
  animation:pulse 2.5s ease-in-out infinite;
  user-select:none;
}
@keyframes pulse{0%,100%{opacity:0.5}50%{opacity:1}}

/* ── ENVELOPE 3D ── */
#envelope-wrap{
  position:relative;
  width:min(340px,88vw);
  cursor:pointer;
  transform-style:preserve-3d;
  filter:drop-shadow(0 30px 60px rgba(0,0,0,0.8));
  transition:transform 0.3s ease;
}
#envelope-wrap:hover{transform:scale(1.03) translateY(-6px);}
#envelope-wrap.opening{cursor:default;pointer-events:none;}

/* Envelope body */
.env-body{
  width:100%;
  aspect-ratio:1.6/1;
  background:linear-gradient(145deg,#1a2a6c 0%,#0d1547 60%,#0a0f2e 100%);
  border-radius:4px 4px 8px 8px;
  position:relative;
  overflow:hidden;
  border:1px solid rgba(201,168,76,0.35);
}
.env-body::before{
  content:'';position:absolute;inset:0;
  background:repeating-linear-gradient(45deg,transparent,transparent 20px,rgba(255,255,255,0.015) 20px,rgba(255,255,255,0.015) 21px);
}
/* Side triangles */
.env-side-l,.env-side-r{
  position:absolute;bottom:0;width:0;height:0;
  border-style:solid;
}
.env-side-l{
  left:0;
  border-width:0 0 100px 170px;
  border-color:transparent transparent rgba(13,21,71,0.95) transparent;
  filter:drop-shadow(2px 0 4px rgba(0,0,0,0.3));
}
.env-side-r{
  right:0;
  border-width:0 170px 100px 0;
  border-color:transparent rgba(13,21,71,0.95) transparent transparent;
  filter:drop-shadow(-2px 0 4px rgba(0,0,0,0.3));
}
/* Bottom flap (V shape) */
.env-bottom{
  position:absolute;bottom:0;left:0;right:0;height:50%;
  background:linear-gradient(170deg,#0f1d5a 0%,#0a1340 100%);
  clip-path:polygon(0 100%,50% 0,100% 100%);
}
/* Wax seal */
.wax-seal{
  position:absolute;
  bottom:-20px;left:50%;transform:translateX(-50%);
  width:60px;height:60px;
  background:radial-gradient(circle at 38% 38%,#e8d48a,#c9a84c 40%,#8b6914 100%);
  border-radius:50%;
  display:flex;align-items:center;justify-content:center;
  font-size:1.5rem;
  box-shadow:0 4px 16px rgba(201,168,76,0.5),inset 0 -2px 6px rgba(0,0,0,0.3);
  z-index:5;
  border:2px solid rgba(201,168,76,0.4);
  animation:sealPulse 3s ease-in-out infinite;
}
@keyframes sealPulse{0%,100%{box-shadow:0 4px 16px rgba(201,168,76,0.5),inset 0 -2px 6px rgba(0,0,0,0.3)}50%{box-shadow:0 4px 30px rgba(201,168,76,0.9),inset 0 -2px 6px rgba(0,0,0,0.3)}}

/* Top flap */
.env-flap{
  position:absolute;top:0;left:0;right:0;
  height:55%;
  transform-origin:top center;
  transform-style:preserve-3d;
  z-index:10;
  transition:transform 0.9s cubic-bezier(0.4,0,0.2,1);
}
.env-flap-inner{
  width:100%;height:100%;
  background:linear-gradient(160deg,#253a8a 0%,#1a2a6c 50%,#0f1d5a 100%);
  clip-path:polygon(0 0,100% 0,50% 100%);
  border-bottom:1px solid rgba(201,168,76,0.2);
  position:relative;
  backface-visibility:hidden;
}
.env-flap-inner::after{
  content:'';position:absolute;inset:0;
  background:linear-gradient(180deg,rgba(255,255,255,0.06) 0%,transparent 60%);
  clip-path:polygon(0 0,100% 0,50% 100%);
}
/* Gold border on envelope */
.env-border{
  position:absolute;inset:4px;
  border:1px solid rgba(201,168,76,0.25);
  border-radius:2px 2px 6px 6px;
  pointer-events:none;
}
/* Monogram on flap */
.env-monogram{
  position:absolute;top:18%;;left:50%;transform:translateX(-50%);
  font-family:'Cinzel',serif;
  font-size:clamp(1.4rem,5vw,2rem);
  color:rgba(201,168,76,0.7);
  text-shadow:0 0 20px rgba(201,168,76,0.4);
  letter-spacing:0.1em;
  z-index:6;pointer-events:none;
}

/* Gold ribbon lines */
.env-ribbon{
  position:absolute;width:100%;
  display:flex;align-items:center;justify-content:center;
  gap:0.5rem;
  z-index:4;pointer-events:none;
}
.env-ribbon.top{top:52%;}
.env-ribbon-line{flex:1;height:1px;background:linear-gradient(90deg,transparent,rgba(201,168,76,0.4),transparent);}
.env-ribbon-diamond{
  width:8px;height:8px;background:var(--gold);
  transform:rotate(45deg);
  box-shadow:0 0 8px rgba(201,168,76,0.6);
}

/* Card inside envelope */
.env-card-preview{
  position:absolute;
  bottom:8%;left:10%;right:10%;
  height:60%;
  background:linear-gradient(160deg,#fdf8f0,#f5e9c4);
  border-radius:4px;
  transform:translateY(100%);
  transition:transform 0.7s cubic-bezier(0.4,0,0.2,1) 0.6s;
  z-index:3;
  display:flex;align-items:center;justify-content:center;
  box-shadow:0 -8px 30px rgba(0,0,0,0.4);
}
.env-card-preview-text{
  font-family:'Cinzel',serif;
  font-size:0.55rem;
  letter-spacing:0.2em;
  color:var(--royal);
  text-align:center;
  opacity:0.7;
}

/* Opening animation states */
#envelope-wrap.opening .env-flap{transform:rotateX(-160deg);}
#envelope-wrap.opening .env-card-preview{transform:translateY(0);}

/* ── SCENE 2: INVITATION CARD (full scroll) ── */
#scene-card{
  position:fixed;inset:0;z-index:20;
  overflow-y:auto;overflow-x:hidden;
  opacity:0;pointer-events:none;
  transition:opacity 0.8s ease;
  background:var(--ink);
}
#scene-card.visible{opacity:1;pointer-events:all;}

/* Particle flicker on card */
#scene-card::before{
  content:'';position:fixed;inset:0;pointer-events:none;z-index:0;
  background:
    radial-gradient(ellipse 80% 50% at 50% 0%,rgba(26,42,108,0.5),transparent 60%),
    radial-gradient(ellipse 60% 40% at 20% 80%,rgba(201,168,76,0.06),transparent 60%);
}

/* ── INVITATION CARD PAPER ── */
.card-paper{
  position:relative;z-index:2;
  max-width:680px;margin:0 auto;
  background:linear-gradient(160deg,#fdfaf4 0%,#f5e9c4 50%,#fdf8f0 100%);
  border-radius:8px;
  margin-top:clamp(2rem,5vw,4rem);
  margin-bottom:clamp(2rem,5vw,4rem);
  padding:clamp(2rem,6vw,4rem) clamp(1.5rem,5vw,3.5rem);
  box-shadow:
    0 40px 100px rgba(0,0,0,0.6),
    0 0 0 1px rgba(201,168,76,0.3),
    inset 0 0 80px rgba(201,168,76,0.04);
  transform:scale(0.85);
  opacity:0;
  transition:transform 1.2s cubic-bezier(0.16,1,0.3,1) 0.3s, opacity 0.8s ease 0.2s;
  overflow:hidden;
}
.card-paper::before{
  content:'';position:absolute;inset:0;pointer-events:none;
  background:
    repeating-linear-gradient(0deg,transparent,transparent 24px,rgba(201,168,76,0.04) 24px,rgba(201,168,76,0.04) 25px),
    repeating-linear-gradient(90deg,transparent,transparent 24px,rgba(201,168,76,0.04) 24px,rgba(201,168,76,0.04) 25px);
  opacity:0.5;
}
.card-paper.revealed{transform:scale(1);opacity:1;}

/* Gold border frame */
.card-border-outer{
  position:absolute;inset:12px;
  border:1px solid rgba(201,168,76,0.45);
  border-radius:4px;
  pointer-events:none;z-index:0;
}
.card-border-inner{
  position:absolute;inset:18px;
  border:1px solid rgba(201,168,76,0.2);
  border-radius:2px;
  pointer-events:none;z-index:0;
}
/* Corner ornaments */
.corner{
  position:absolute;width:24px;height:24px;
  z-index:1;pointer-events:none;
}
.corner svg{fill:none;stroke:var(--gold);stroke-width:1.5;}
.corner.tl{top:22px;left:22px;}
.corner.tr{top:22px;right:22px;transform:rotate(90deg);}
.corner.bl{bottom:22px;left:22px;transform:rotate(-90deg);}
.corner.br{bottom:22px;right:22px;transform:rotate(180deg);}

/* Card content */
.card-content{position:relative;z-index:2;text-align:center;}

.card-overline{
  font-family:'Cinzel',serif;
  font-size:clamp(0.55rem,1.5vw,0.7rem);
  letter-spacing:0.4em;
  color:var(--gold);
  margin-bottom:1.2rem;
}
.card-name{
  font-family:'Cinzel Decorative',serif;
  font-size:clamp(2rem,8vw,3.8rem);
  color:var(--royal);
  line-height:1.1;
  text-shadow:0 2px 8px rgba(26,42,108,0.15);
  margin-bottom:0.3rem;
}
.card-name span{color:var(--gold);display:block;}
.card-eighteenth{
  font-family:'Cormorant Garamond',serif;
  font-style:italic;
  font-size:clamp(1rem,3vw,1.4rem);
  color:var(--royal);
  letter-spacing:0.15em;
  margin-bottom:0.5rem;
}
.card-debut{
  font-family:'Cinzel',serif;
  font-size:clamp(0.6rem,2vw,0.8rem);
  letter-spacing:0.5em;
  color:var(--sapphire);
}

/* Divider */
.card-divider{
  display:flex;align-items:center;gap:0.8rem;
  margin:1.5rem 0;
}
.div-line{flex:1;height:1px;background:linear-gradient(90deg,transparent,var(--gold),transparent);}
.div-gem{
  width:10px;height:10px;
  background:linear-gradient(135deg,var(--gold-light),var(--gold));
  transform:rotate(45deg);
  box-shadow:0 0 8px rgba(201,168,76,0.5);
}

/* Invitation body text */
.card-body{
  font-family:'Cormorant Garamond',serif;
  font-size:clamp(1rem,2.5vw,1.2rem);
  line-height:2;
  color:#2a3060;
}
.card-body em{font-style:italic;color:var(--royal);}

/* Detail pills */
.detail-block{
  margin:2rem auto;
  max-width:420px;
  background:linear-gradient(135deg,rgba(26,42,108,0.06),rgba(201,168,76,0.08));
  border:1px solid rgba(201,168,76,0.3);
  border-radius:8px;
  padding:1.5rem;
  display:grid;grid-template-columns:1fr 1fr;gap:1.2rem;
}
.detail-item{text-align:center;}
.detail-label{
  font-family:'Cinzel',serif;
  font-size:0.55rem;letter-spacing:0.3em;
  color:var(--gold);margin-bottom:0.3rem;
}
.detail-value{
  font-family:'Cormorant Garamond',serif;
  font-size:clamp(0.95rem,2.5vw,1.1rem);
  color:var(--royal);font-weight:600;
  line-height:1.4;
}
.detail-item.full{grid-column:1/-1;}

/* ── DRESS CODE ── */
.dress-section{
  background:linear-gradient(135deg,rgba(26,42,108,0.05),rgba(201,168,76,0.06));
  border:1px solid rgba(201,168,76,0.25);
  border-radius:8px;
  padding:1.5rem;
  margin:2rem 0;
}
.dress-title{
  font-family:'Cinzel',serif;
  font-size:0.6rem;letter-spacing:0.35em;
  color:var(--gold);margin-bottom:1rem;
}
.dress-grid{
  display:flex;flex-wrap:wrap;justify-content:center;gap:0.5rem;
}
.dress-pill{
  display:inline-flex;align-items:center;gap:0.45rem;
  padding:0.4rem 0.9rem;
  border-radius:50px;
  border:1px solid rgba(26,42,108,0.2);
  background:rgba(26,42,108,0.06);
  font-family:'Montserrat',sans-serif;
  font-size:0.72rem;
  color:#1a2a6c;
  font-weight:500;
}
.swatch{width:11px;height:11px;border-radius:50%;flex-shrink:0;}

/* ── GIFT SECTION ── */
.gift-section{
  background:linear-gradient(135deg,rgba(201,168,76,0.08),rgba(26,42,108,0.04));
  border:1px solid rgba(201,168,76,0.3);
  border-radius:8px;
  padding:1.8rem;
  margin:2rem 0;
  text-align:center;
}
.gift-quote{
  font-family:'Cormorant Garamond',serif;
  font-style:italic;
  font-size:clamp(0.95rem,2.5vw,1.1rem);
  color:#2a3060;line-height:1.9;
  margin-bottom:1rem;
}
.gift-tags{
  display:flex;flex-wrap:wrap;justify-content:center;gap:0.5rem;
  margin-top:1rem;
}
.gift-tag{
  padding:0.35rem 0.85rem;
  border-radius:50px;
  border:1px solid rgba(201,168,76,0.4);
  background:rgba(201,168,76,0.08);
  font-family:'Montserrat',sans-serif;
  font-size:0.7rem;color:var(--royal);
}
.gcash-box{
  margin-top:1.2rem;padding:0.75rem 1.2rem;
  background:rgba(26,42,108,0.06);
  border-radius:6px;border:1px dashed rgba(201,168,76,0.35);
  font-family:'Montserrat',sans-serif;
  font-size:0.78rem;color:#2a3060;
}
.gcash-box strong{color:var(--royal);font-weight:600;}

/* ── MAP SECTION ── */
.map-section{
  margin:2rem 0 0;
  border-radius:8px;overflow:hidden;
  border:1px solid rgba(201,168,76,0.3);
  box-shadow:0 8px 30px rgba(0,0,0,0.15);
}
.map-header{
  background:linear-gradient(135deg,var(--royal),var(--sapphire));
  padding:0.8rem 1.2rem;
  display:flex;align-items:center;gap:0.6rem;
}
.map-header-text{
  font-family:'Cinzel',serif;
  font-size:0.65rem;letter-spacing:0.2em;color:var(--gold-light);
}
.map-header-sub{
  font-family:'Montserrat',sans-serif;
  font-size:0.65rem;color:rgba(255,255,255,0.6);margin-top:0.15rem;
}
.map-frame-wrap{position:relative;width:100%;height:280px;}
.map-frame-wrap iframe{width:100%;height:100%;border:0;display:block;}
.map-cta{
  background:var(--royal);
  padding:0.75rem;text-align:center;
}
.map-cta a{
  font-family:'Cinzel',serif;
  font-size:0.65rem;letter-spacing:0.25em;
  color:var(--gold-light);text-decoration:none;
  transition:color 0.2s;
}
.map-cta a:hover{color:white;}

/* Card footer */
.card-footer{
  margin-top:2rem;padding-top:1.5rem;
  border-top:1px solid rgba(201,168,76,0.2);
  text-align:center;
}
.card-footer-name{
  font-family:'Cinzel Decorative',serif;
  font-size:clamp(0.9rem,3vw,1.2rem);
  color:var(--royal);
}
.card-footer-sub{
  font-family:'Cormorant Garamond',serif;
  font-style:italic;
  font-size:0.9rem;color:rgba(26,42,108,0.6);
  margin-top:0.3rem;
}

/* Stars on dark bg */
.star{
  position:fixed;border-radius:50%;background:white;
  animation:twk var(--d,3s) ease-in-out infinite var(--dl,0s);
  opacity:0;pointer-events:none;z-index:1;
}
@keyframes twk{0%,100%{opacity:0}50%{opacity:var(--op,0.7)}}

/* ── FLOATING PETALS ── */
.petal{
  position:fixed;pointer-events:none;z-index:1;
  width:8px;height:8px;
  background:radial-gradient(circle,rgba(201,168,76,0.6),transparent);
  border-radius:50% 0 50% 0;
  animation:floatPetal var(--pd,8s) ease-in-out infinite var(--pdl,0s);
  opacity:0;
}
@keyframes floatPetal{
  0%{opacity:0;transform:translate(0,0) rotate(0deg);}
  10%{opacity:0.7;}
  90%{opacity:0.3;}
  100%{opacity:0;transform:translate(var(--px,30px),calc(-100vh - 50px)) rotate(360deg);}
}

/* ── OPEN BUTTON (back) ── */
.back-btn{
  position:fixed;bottom:1.5rem;right:1.5rem;z-index:100;
  background:linear-gradient(135deg,var(--royal),var(--ink));
  border:1px solid rgba(201,168,76,0.4);
  border-radius:50px;
  padding:0.6rem 1.2rem;
  font-family:'Cinzel',serif;
  font-size:0.6rem;letter-spacing:0.2em;
  color:var(--gold);cursor:pointer;
  box-shadow:0 4px 20px rgba(0,0,0,0.5);
  transition:all 0.2s;
  display:none;
}
.back-btn:hover{background:linear-gradient(135deg,var(--sapphire),var(--royal));transform:translateY(-2px);}

/* ── CLICK RIPPLE ── */
@keyframes ripple{0%{transform:translate(-50%,-50%) scale(0);opacity:0.6;}100%{transform:translate(-50%,-50%) scale(6);opacity:0;}}
.ripple-el{
  position:fixed;border-radius:50%;
  width:80px;height:80px;
  background:radial-gradient(circle,rgba(201,168,76,0.3),transparent);
  pointer-events:none;z-index:15;
  animation:ripple 0.8s ease-out forwards;
}

/* Scroll indicator */
.scroll-hint{
  position:fixed;bottom:1.5rem;left:50%;transform:translateX(-50%);
  z-index:50;
  display:flex;flex-direction:column;align-items:center;gap:0.4rem;
  font-family:'Cinzel',serif;font-size:0.55rem;
  letter-spacing:0.25em;color:rgba(201,168,76,0.7);
  animation:fadeInUp 1s ease 2s both;
  pointer-events:none;
}
@keyframes fadeInUp{from{opacity:0;transform:translate(-50%,10px)}to{opacity:1;transform:translate(-50%,0)}}
.scroll-arrow{animation:bob 1.5s ease-in-out infinite;}
@keyframes bob{0%,100%{transform:translateY(0)}50%{transform:translateY(5px)}}

/* ── COUNTDOWN ── */
.countdown-section{
  margin:2rem 0;
  text-align:center;
}
.countdown-label{
  font-family:'Cinzel',serif;
  font-size:0.6rem;letter-spacing:0.4em;
  color:var(--gold);margin-bottom:1rem;
}
.countdown-grid{
  display:flex;justify-content:center;gap:0.8rem;flex-wrap:wrap;
}
.cd-box{
  background:linear-gradient(145deg,var(--royal),var(--ink));
  border:1px solid rgba(201,168,76,0.4);
  border-radius:10px;
  padding:0.9rem 0.7rem 0.6rem;
  min-width:68px;
  box-shadow:0 4px 20px rgba(0,0,0,0.25),inset 0 1px 0 rgba(255,255,255,0.06);
}
.cd-num{
  font-family:'Cinzel',serif;
  font-size:clamp(1.6rem,5vw,2.2rem);
  font-weight:600;
  color:var(--gold-light);
  line-height:1;
  text-shadow:0 0 20px rgba(201,168,76,0.5);
}
.cd-lbl{
  font-family:'Montserrat',sans-serif;
  font-size:0.5rem;letter-spacing:0.25em;
  color:rgba(192,200,216,0.6);
  margin-top:0.35rem;
  text-transform:uppercase;
}
.cd-sep{
  font-family:'Cinzel',serif;
  font-size:1.4rem;color:var(--gold);
  align-self:center;margin-bottom:0.4rem;
  opacity:0.6;
  animation:sepBlink 1s ease-in-out infinite;
}
@keyframes sepBlink{0%,100%{opacity:0.6}50%{opacity:0.15}}

/* ── COUNTDOWN ── */
.fade-section{opacity:0;transform:translateY(20px);transition:opacity 0.7s ease,transform 0.7s ease;}
.fade-section.in{opacity:1;transform:translateY(0);}
</style>
</head>
<body>

<!-- Stars -->
<div id="starfield"></div>

<!-- ══════════ SCENE 1: ENVELOPE ══════════ -->
<div id="scene-envelope">
  <p class="prompt-text">✦ &nbsp; tap to open your invitation &nbsp; ✦</p>

  <div id="envelope-wrap" onclick="openEnvelope(event)">
    <div class="env-body">
      <div class="env-border"></div>
      <!-- Gold V lines -->
      <div class="env-ribbon top">
        <div class="env-ribbon-line"></div>
        <div class="env-ribbon-diamond"></div>
        <div class="env-ribbon-line"></div>
      </div>
      <div class="env-side-l"></div>
      <div class="env-side-r"></div>
      <div class="env-bottom"></div>
      <!-- Card peeking out -->
      <div class="env-card-preview">
        <div class="env-card-preview-text">RHIANA C. SADIA<br/>18TH DEBUT</div>
      </div>
    </div>
    <!-- Animated flap -->
    <div class="env-flap">
      <div class="env-flap-inner"></div>
    </div>
    <!-- Monogram (visible before opening) -->
    <div class="env-monogram">RS</div>
    <!-- Wax seal -->
    <div class="wax-seal">✦</div>
  </div>

  <p style="font-family:'Cormorant Garamond',serif;font-style:italic;font-size:0.85rem;color:rgba(192,200,216,0.45);letter-spacing:0.1em;">
    An evening of midnight elegance awaits
  </p>
</div>

<!-- ══════════ SCENE 2: INVITATION CARD ══════════ -->
<div id="scene-card">
  <div style="padding:0 clamp(1rem,4vw,2rem);position:relative;z-index:2;">
    <div class="card-paper" id="card-paper">

      <!-- Frame decorations -->
      <div class="card-border-outer"></div>
      <div class="card-border-inner"></div>
      <div class="corner tl"><svg viewBox="0 0 24 24"><path d="M20 4H4v16"/><path d="M4 4l8 8"/></svg></div>
      <div class="corner tr"><svg viewBox="0 0 24 24"><path d="M20 4H4v16"/><path d="M4 4l8 8"/></svg></div>
      <div class="corner bl"><svg viewBox="0 0 24 24"><path d="M20 4H4v16"/><path d="M4 4l8 8"/></svg></div>
      <div class="corner br"><svg viewBox="0 0 24 24"><path d="M20 4H4v16"/><path d="M4 4l8 8"/></svg></div>

      <div class="card-content">

        <!-- Header -->
        <div class="fade-section">
          <p class="card-overline">✦ &nbsp; you are cordially invited &nbsp; ✦</p>
          <p class="card-eighteenth">to the eighteenth birthday of</p>

          <div class="card-divider"><div class="div-line"></div><div class="div-gem"></div><div class="div-line"></div></div>

          <h1 class="card-name">
            Rhiana
            <span>C. Sadia</span>
          </h1>

          <div class="card-divider"><div class="div-line"></div><div class="div-gem"></div><div class="div-gem" style="margin:0 -4px"></div><div class="div-gem"></div><div class="div-line"></div></div>

          <p class="card-debut">DEBUT &nbsp;·&nbsp; DECEMBER 20, 2026</p>
        </div>

        <!-- Invitation text -->
        <div class="fade-section" style="margin:2rem 0;">
          <p class="card-body">
            Together with their beloved family,<br/>
            <em>Mr. &amp; Mrs. Sadia</em><br/>
            joyfully request the pleasure of your company<br/>
            as their daughter takes her first steps<br/>
            into the grace and wonder of womanhood.
          </p>
          <p class="card-body" style="margin-top:1rem;">
            Join us beneath the midnight stars<br/>
            for an evening wrapped in elegance,<br/>
            laughter, and unforgettable memories.
          </p>
        </div>

        <!-- Details -->
        <div class="fade-section">
          <div class="detail-block">
            <div class="detail-item">
              <div class="detail-label">📅 DATE</div>
              <div class="detail-value">December 20, 2026</div>
            </div>
            <div class="detail-item">
              <div class="detail-label">⏰ TIME</div>
              <div class="detail-value">5:00 in the Evening</div>
            </div>
            <div class="detail-item full">
              <div class="detail-label">📍 VENUE</div>
              <div class="detail-value">NDN and Events Place<br/>
                <span style="font-size:0.85em;font-weight:400;color:rgba(26,42,108,0.7);">32VR+26M, Talisay, Batangas</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Countdown -->
        <div class="fade-section">
          <div class="countdown-section">
            <p class="countdown-label">✦ &nbsp; COUNTING DOWN TO THE BIG DAY &nbsp; ✦</p>
            <div class="countdown-grid">
              <div class="cd-box"><div class="cd-num" id="cd-days">000</div><div class="cd-lbl">Days</div></div>
              <div class="cd-sep">:</div>
              <div class="cd-box"><div class="cd-num" id="cd-hours">00</div><div class="cd-lbl">Hours</div></div>
              <div class="cd-sep">:</div>
              <div class="cd-box"><div class="cd-num" id="cd-mins">00</div><div class="cd-lbl">Mins</div></div>
              <div class="cd-sep">:</div>
              <div class="cd-box"><div class="cd-num" id="cd-secs">00</div><div class="cd-lbl">Secs</div></div>
            </div>
          </div>
        </div>

        <!-- Dress Code -->
        <div class="fade-section">
          <div class="dress-section">
            <p class="dress-title">✦ &nbsp; DRESS CODE &nbsp; ✦</p>
            <div class="dress-grid">
              <span class="dress-pill"><span class="swatch" style="background:#1a1a1a;border:1px solid #444;"></span>Black – Classic &amp; Formal</span>
              <span class="dress-pill"><span class="swatch" style="background:#f5f5f0;border:1px solid #ccc;"></span>White / Off-White</span>
              <span class="dress-pill"><span class="swatch" style="background:#a0a8b8;"></span>Silver / Gray</span>
              <span class="dress-pill"><span class="swatch" style="background:#d4b896;"></span>Nude / Champagne / Beige</span>
              <span class="dress-pill"><span class="swatch" style="background:linear-gradient(135deg,#c0c8d8,#c9a84c);"></span>Metallics</span>
            </div>
            <p style="margin-top:1rem;font-family:'Cormorant Garamond',serif;font-style:italic;font-size:0.85rem;color:rgba(26,42,108,0.6);">
              Kindly reserve <strong style="font-style:normal;color:var(--royal);">Midnight Blue</strong> for the debutante ✦
            </p>
          </div>
        </div>

        <!-- Gift Registry -->
        <div class="fade-section">
          <div class="gift-section">
            <p class="card-overline" style="margin-bottom:0.8rem;">✦ &nbsp; GIFT REGISTRY &nbsp; ✦</p>
            <p class="gift-quote">
              "Your presence is the greatest gift of all —<br/>
              no offering is required, only your warm company."
            </p>
            <p style="font-family:'Montserrat',sans-serif;font-size:0.75rem;color:rgba(26,42,108,0.65);">
              However, if you wish to bless Rhiana, she would love:
            </p>
            <div class="gift-tags">
              <span class="gift-tag">💳 GCash</span>
              <span class="gift-tag">💄 Skincare &amp; Beauty</span>
              <span class="gift-tag">🎁 Gift Cards</span>
              <span class="gift-tag">🧱 Lego</span>
              <span class="gift-tag">👗 Clothes (M or L)</span>
              <span class="gift-tag">👟 Shoes (Size 8.5)</span>
              <span class="gift-tag">👜 Bag</span>
              <span class="gift-tag">💍 Silver Jewelries</span>
            </div>
            <div class="gcash-box">
              GCash: <strong>09XX-XXX-XXXX</strong> &nbsp;·&nbsp; Rhiana C. Sadia
            </div>
          </div>
        </div>

        <!-- Map -->
        <div class="fade-section">
          <div class="map-section">
            <div class="map-header">
              <svg width="16" height="16" fill="none" stroke="var(--gold-light)" stroke-width="2" viewBox="0 0 24 24" style="flex-shrink:0"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7z"/><circle cx="12" cy="9" r="2.5"/></svg>
              <div>
                <div class="map-header-text">NDN AND EVENTS PLACE</div>
                <div class="map-header-sub">32VR+26M, Talisay, Batangas</div>
              </div>
            </div>
            <div class="map-frame-wrap">
              <iframe
                src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3869.584!2d121.0379568!3d14.0925979!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x33bd706e4f1fe8d7%3A0x24f6029a92527177!2sNDN%20Resort%20And%20Events%20Place!5e0!3m2!1sen!2sph!4v1700000000000!5m2!1sen!2sph"
                allowfullscreen=""
                loading="lazy"
                referrerpolicy="no-referrer-when-downgrade"
                style="width:100%;height:100%;border:0;display:block;"
                title="NDN Resort and Events Place, Talisay Batangas">
              </iframe>
            </div>
            <div class="map-cta">
              <a href="https://www.google.com/maps/place/NDN+Resort+And+Events+Place/@14.0925979,121.0379568,17z" target="_blank">
                ✦ &nbsp; OPEN IN GOOGLE MAPS &nbsp; ✦
              </a>
            </div>
          </div>
        </div>

        <!-- Footer -->
        <div class="fade-section">
          <div class="card-footer">
            <div class="card-divider"><div class="div-line"></div><div class="div-gem"></div><div class="div-gem" style="margin:0 -4px"></div><div class="div-gem"></div><div class="div-line"></div></div>
            <p class="card-footer-name">Rhiana C. Sadia</p>
            <p class="card-footer-sub">18th Birthday Debut &nbsp;·&nbsp; December 20, 2026</p>
            <p style="margin-top:1rem;font-family:'Cinzel',serif;font-size:0.5rem;letter-spacing:0.3em;color:rgba(201,168,76,0.5);">
              ✦ &nbsp; CRAFTED WITH LOVE &nbsp; ✦
            </p>
          </div>
        </div>

      </div><!-- /card-content -->
    </div><!-- /card-paper -->
  </div><!-- /padding wrap -->

  <!-- Scroll hint -->
  <div class="scroll-hint" id="scroll-hint">
    <span>SCROLL</span>
    <div class="scroll-arrow">
      <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
    </div>
  </div>
</div><!-- /scene-card -->

<!-- Back button -->
<button class="back-btn" id="back-btn" onclick="backToEnvelope()">↩ ENVELOPE</button>

<script>
// ── Stars ──
const sf = document.getElementById('starfield');
for(let i=0;i<200;i++){
  const s=document.createElement('div');
  s.className='star';
  const sz=Math.random()*2+0.4;
  s.style.cssText=`width:${sz}px;height:${sz}px;left:${Math.random()*100}%;top:${Math.random()*100}%;--d:${(Math.random()*4+1.5).toFixed(1)}s;--dl:${(Math.random()*6).toFixed(1)}s;--op:${(Math.random()*0.8+0.2).toFixed(2)}`;
  sf.appendChild(s);
}

// ── Petals ──
for(let i=0;i<20;i++){
  const p=document.createElement('div');
  p.className='petal';
  const left=Math.random()*100;
  const dx=(Math.random()-0.5)*120;
  p.style.cssText=`left:${left}%;top:${90+Math.random()*20}%;--pd:${(Math.random()*10+6).toFixed(1)}s;--pdl:${(Math.random()*10).toFixed(1)}s;--px:${dx}px;transform:rotate(${Math.random()*360}deg)`;
  document.body.appendChild(p);
}

// ── Ripple ──
function spawnRipple(x,y){
  const r=document.createElement('div');
  r.className='ripple-el';
  r.style.cssText=`left:${x}px;top:${y}px;`;
  document.body.appendChild(r);
  setTimeout(()=>r.remove(),800);
}

// ── Open envelope ──
let opened=false;
function openEnvelope(e){
  if(opened)return;
  opened=true;
  const rect=e.currentTarget.getBoundingClientRect();
  spawnRipple(rect.left+rect.width/2,rect.top+rect.height/2);

  const wrap=document.getElementById('envelope-wrap');
  wrap.classList.add('opening');

  // After flap opens, fade to card
  setTimeout(()=>{
    const scene2=document.getElementById('scene-card');
    const paper=document.getElementById('card-paper');
    scene2.classList.add('visible');
    document.getElementById('scene-envelope').style.pointerEvents='none';

    setTimeout(()=>{
      paper.classList.add('revealed');
      document.getElementById('back-btn').style.display='block';

      // Fade in sections with stagger
      const sections=document.querySelectorAll('.fade-section');
      sections.forEach((s,i)=>{
        setTimeout(()=>s.classList.add('in'),400+i*180);
      });

      // Hide scroll hint after scroll
      scene2.addEventListener('scroll',()=>{
        document.getElementById('scroll-hint').style.opacity='0';
      },{once:true,passive:true});
    },200);
  },1100);
}

// ── Back to envelope ──
function backToEnvelope(){
  const scene2=document.getElementById('scene-card');
  const paper=document.getElementById('card-paper');
  paper.classList.remove('revealed');
  scene2.classList.remove('visible');
  scene2.scrollTop=0;
  setTimeout(()=>{
    const wrap=document.getElementById('envelope-wrap');
    wrap.classList.remove('opening');
    document.getElementById('scene-envelope').style.pointerEvents='';
    document.getElementById('back-btn').style.display='none';
    document.querySelectorAll('.fade-section').forEach(s=>s.classList.remove('in'));
    opened=false;
  },500);
}

// ── Countdown ──
function updateCountdown(){
  const target=new Date('2026-12-20T17:00:00+08:00').getTime();
  const diff=target-Date.now();
  if(diff<=0){
    ['cd-days','cd-hours','cd-mins','cd-secs'].forEach(id=>document.getElementById(id).textContent='00');
    return;
  }
  const d=Math.floor(diff/86400000);
  const h=Math.floor((diff%86400000)/3600000);
  const m=Math.floor((diff%3600000)/60000);
  const s=Math.floor((diff%60000)/1000);
  document.getElementById('cd-days').textContent=String(d).padStart(3,'0');
  document.getElementById('cd-hours').textContent=String(h).padStart(2,'0');
  document.getElementById('cd-mins').textContent=String(m).padStart(2,'0');
  document.getElementById('cd-secs').textContent=String(s).padStart(2,'0');
}
updateCountdown();
setInterval(updateCountdown,1000);

// ── Intersection observer for card sections ──
const io=new IntersectionObserver(entries=>{
  entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in');});
},{threshold:0.12});
document.querySelectorAll('.fade-section').forEach(s=>io.observe(s));
</script>
</body>
</html>
